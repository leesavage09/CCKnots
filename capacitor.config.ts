import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'uk.co.leesavage.CCknots',
  appName: 'sailingknots3d',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
